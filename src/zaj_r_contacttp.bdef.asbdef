managed implementation in class zbp_aj_rh_contact unique;
strict ( 2 );

with draft;

auxiliary class zcl_aj_rh_contact_behavior;

define behavior for ZAJ_R_CONTACTTP alias Contact
persistent table zaj_rh_contact
draft table zaj_rh_contact_d
lock master total etag LastChangedAt
authorization master ( instance )
etag master LocalLastChangedAt
{
  create ( authorization : global );
  update;
  delete;

  field ( readonly ) ContactId;
  field ( features : instance ) Country;

  draft action Edit;
  draft action Activate optimized;
  draft action Discard;
  draft action Resume;
  draft determine action Prepare;

  static factory action ( authorization : global ) createAddress [1];
  static factory action ( authorization : global ) createCustomer [1];
  static factory action ( authorization : global ) createEmployee [1];

  mapping for zaj_rh_contact
    {
      ContactId          = contact_id;
      ContactTypeId      = contact_type;
      FirstName          = first_name;
      LastName           = last_name;
      Birthday           = birthday;
      Street             = street;
      HouseNumber        = house_number;
      Town               = town;
      ZipCode            = zip_code;
      Country            = country;
      Telephone          = telephone;
      Email              = email;
      LocalCreatedBy     = local_created_by;
      LocalCreatedAt     = local_created_at;
      LocalLastChangedBy = local_last_changed_by;
      LocalLastChangedAt = local_last_changed_at;
      LastChangedAt      = last_changed_at;
    }
}